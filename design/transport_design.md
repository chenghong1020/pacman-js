## 需求
### 需求分析
1. **功能需求**：
   - 实现Pacman的传送功能，包括入口/出口
   - 传送门应在迷宫图中提前设计好位置，且配对关系固定
   - 传送门应在使用后禁用一段时间
   - 传送后Pacman应保持原方向

## 整体思路

### 整体修改思路（继承设计文档架构）

#### 设计继承点分析：
1. **事件驱动架构**：复用现有事件总线机制，通过`teleport`事件解耦功能模块
2. **实体组件模式**：新增`Portal`作为独立实体类，继承Pickup的资源管理特性
3. **分层状态管理**：在GamePlayer层处理传送逻辑，保持Pacman的纯净移动控制
4. **配置数据驱动**：通过扩展迷宫数据定义实现关卡设计

---

### 修改方案分解为四大模块

#### 模块一：数据层扩展（Map Data）
**目标**：在迷宫数据中定义传送门位置及配对关系


#### 模块二：实体系统扩展（Entity System）
**目标**：实现传送门实体及其交互


#### 模块三：游戏逻辑扩展（Game Logic）
**目标**：实现传送行为控制


#### 模块四：表现层扩展（Presentation）
**目标**：传送视觉效果

# 分模块详细设计


### 模块一：数据层扩展详细设计

#### 1. 修改 GameUtilities 类，修改迷宫数据支持传送门


#### 2. 修改 GameCoordinator 类，初始化迷宫


#### 3. 新增 Portal 实体类，继承Pickup


### 模块二：实体系统
#### 修改Portal类
**核心组件**：
1. Portal类 实现传送门实体逻辑
   - 配对坐标、冷却状态
   - 碰撞检测、触发传送事件


### 模块三：游戏逻辑
#### 修改GamePlayer类
**流程控制**：
1. 事件响应机制
   - 监听传送事件
   - 验证冷却状态和位置可达性
2. 传送执行逻辑
   - 坐标对齐网格
   - 方向保持处理
   - 启动冷却

**状态管理**：
- 本地冷却：单个传送门3秒禁用
- 全局冷却：1秒内禁止二次传送

#### 修改 Pacman类， 支持传送逻辑


### 模块四：表现层
#### 修改Portal类，支持传送视觉效果
**视觉要素**：
1. 传送门动态效果
   - 冷却动画
#### 修改 Pacman类，支持传送视觉效果
2. Pacman传送表现
   - 平滑效果
#### 修改 SoundManager，支持传送音效 
---